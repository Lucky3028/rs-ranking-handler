name: Rust

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
    # IncrementalBuildが機能しないので、以下2ステップでファイルのタイムスタンプをgitから復元する
    # https://zenn.dev/kt3k/articles/d557cc874961ab
    - name: Install Python
      uses: actions/setup-python@v2
    - name: Restore mtime
      run: python ./git-restore-mtime.py
    - name: Restore cache
      uses: actions/cache@v2
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: cargo-${{ github.ref }}-${{ hashFiles('**/Cargo.lock') }}
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
